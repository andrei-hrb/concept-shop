mixin button($section, $content, $link)
    - $classes = ["button", $section . "__button", "vertical-text"]
    div(class=$classes)
        div
            a(href=$link)=$content
        div
            a(href=$link)=$content